<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.stephen.guess">

    <uses-feature
        android:name="android.hardware.camera2"
        android:required="true" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Guess"
        android:usesCleartextTraffic="true">
        <activity android:name=".view.activity.NewsActivity"></activity>
        <activity android:name=".view.activity.SnookerActivity" />
        <activity android:name=".view.activity.RecordListActivity" />
        <activity android:name=".view.activity.ListActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.activity.RecordActivity" />
        <activity android:name=".view.activity.MainActivity" />
    </application>

</manifest>